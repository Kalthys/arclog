import 'package:flutter/material.dart';

abstract final class ArclogColors {
  // Backgrounds
  static const Color deepBlack = Color(0xFF050A0E);
  static const Color surfaceDark = Color(0xFF0D1F2D);
  static const Color circuitLine = Color(0xFF1A2C38);

  // Neon accents
  static const Color cyanGlow = Color(0xFF00FBFF);
  static const Color electricYellow = Color(0xFFFFD700);

  // Semantic
  static const Color success = Color(0xFF39FF14);
  static const Color error = Color(0xFFFF2D55);
  static const Color textPrimary = Color(0xFFE0F7FA);
  static const Color textSecondary = Color(0xFF607D8B);
}
